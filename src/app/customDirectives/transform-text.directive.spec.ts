import { TransformTextDirective } from './transform-text.directive';

describe('TransformTextDirective', () => {
  it('should create an instance', () => {
    const directive = new TransformTextDirective();
    expect(directive).toBeTruthy();
  });
});
